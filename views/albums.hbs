<h1 class="heading">Albums</h1>
<section class="data-section">
    {{#each albums}}
    <div class="data-card">
        <img src="{{this.images.[1].url}}">
        <h2>{{this.name}}</h2>
        <a class="btn" href="/tracks/{{this.id}}">View Tracks</a>
    </div>
    {{/each}}
</section>
